[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "yacli"
version = "0.1.0"
description = "Yet Another Command Line Interface"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
yacli = "yacli.main:cli_entry"
